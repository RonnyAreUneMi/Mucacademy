{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "until python manage.py migrate --noinput; do echo 'DB no lista, reintento en 3s...'; sleep 3; done && (python manage.py import_data || true) && gunicorn config.wsgi:application --bind [::]:8000 --workers 1 --timeout 120 --log-file - --access-logfile - --log-level info",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
