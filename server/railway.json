{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && (timeout 30 python manage.py create_default_superuser || true) && gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 2 --timeout 120 --log-file - --access-logfile - --log-level info",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
