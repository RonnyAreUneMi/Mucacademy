venv/
.venv/
env/
.env/
db.sqlite3
__pycache__/
*.pyc
.BS_Store
.DS_Store
.idea/
.vscode/
*.log
.env
staticfiles/

# === MEDIA: archivos subidos en runtime — NUNCA se commitean ===
# Ignora TODA la carpeta media (raíz y server/), asi ningun logo/banner/PDF/QR
# entra al repo. La persistencia va por almacenamiento externo (Cloudinary/Volume),
# no por git.
media/
server/media/
/media/

# Celery Beat scheduler (estado regenerable, no commitear)
celerybeat-schedule
celerybeat-schedule.*
celerybeat.pid
.backup-pre-refactor/
